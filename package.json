{
  "name": "srms-application",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "install": "cd frontend && npm ci",
    "start": "cd frontend && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}